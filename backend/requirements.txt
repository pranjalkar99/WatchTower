fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
pydantic>=2.9.0
httpx>=0.27.0

# Optional — Gemini security agent + embeddings (LLM_PROVIDER=gemini)
google-genai>=1.0.0

# Benchmarking (pip install -r requirements.txt)
datasets>=3.0.0

# Optional — for real LangGraph + OpenAI (pip install -r requirements-agent.txt)
# langgraph langchain-core langchain-openai
